

### 概述
初始加载Cesium地球会有等待时间，为了提升用户体验，可以添加loading效果。



```javascript
viewer.scene.globe.tileLoadProgressEvent.addEventListener((remaining) => {
  // 当remaining为0时，表示加载完成
  if (remaining === 0) {}
})
```

